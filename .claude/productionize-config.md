# Productionize Config

Generated by `/productionize:strategy` on 2026-03-29

## Strategy
- **Product:** Venture Scale
- **Hypothesis:** I believe VC associates and partners who screen high volumes of pitch decks will find this useful because it turns 3-5 minutes of manual evaluation into a structured, consistent analysis in under 60 seconds — and every analysis forwarded to a colleague becomes a product demo.
- **Ideal User:** A VC associate screening 50-200 decks per month who needs to quickly separate the fundable from the clearly-not, and produce structured summaries for partner review.
- **First Strike:** Upload a pitch deck PDF, pick the stage, and receive a scored analysis with Verified/Quick Win/Gap breakdown — in under 60 seconds, no signup required.
- **KUI:** 3+ deck analyses completed (user has integrated it into their screening workflow, not just kicked the tires).
- **Core Offer:**
  - Result: Structured, investor-grade pitch deck evaluations that save 80%+ of screening time, with actionable recommendations separated into "deck edits" and "business changes"
  - Advantage: The only tool with a 45-question stage-gated checklist and Verified/Quick Win/Gap classification — built from real investor evaluation work, not simulated
  - Assurance: Free to use, no signup for first analysis, decks ephemeral (deleted after analysis), results in under 60 seconds

## First Strike Path
1. Landing page — live demo area is the hero; user sees an example analysis immediately (value before signup)
2. Upload PDF + pick stage — single form: drag-drop deck, select Pre-Seed/Seed/Series A+ (1 click)
3. Processing — ~60 seconds, progress indicator
4. See scored analysis — interactive results page with full Verified/Quick Win/Gap breakdown
Clicks to value: 2

Signup (email via magic link) gates the second analysis, not the first.

## Decisions
- **Framework:** Hono + htmx
- **Deploy:** Cloudflare Workers (+ Pages for static assets)
- **Auth:** Magic Link via Resend
- **Analytics:** PostHog (funnels, session replay, cohort analysis)
- **Database:** Cloudflare D1 (SQLite) + R2 (object storage for PDFs and result HTML)
- **Intro Video:** Screenshot GIF (autoplay walkthrough of upload → results flow)
- **Template Split:** 80/20 (template: page structure, auth, analytics, deploy. Custom: core feature, results page, demo data, VC copy)

## Notes
- The interactive HTML results template already exists (`.claude/skills/venture-scale/template.html`) — reuse it for the web app results page.
- COGS are ~$0.08-0.12 per analysis (Sonnet with caching). Margins are healthy at any price point.
- Phase 1 distribution is personal DMs to 10-15 VCs, not marketing. The product is a relationship-opener first.
- Kill criteria: nobody uses it after sharing with 10-15 VCs directly.
- Go criteria: 5+ repeat users, feature requests for batch upload, 2+ new VC relationships opened.
- Comprehensive GTM research available at `gtm-research/` — synthesis, competitive landscape, distribution channels, business analysis, and validation plan.

## Learnings (2026-03-29 build session)

**Time spent:** ~5.5 hours total. ~3 hours productive build, ~2.5 hours infrastructure debugging.

**What went well:**
- Landing page as embedded product demo (not marketing sections) — validated immediately, user loved it
- Using a real recognizable deck (Airbnb) as the sample analysis made the demo feel credible
- The `/venture-scale` Claude Code skill already had the full checklist prompt — porting it to an API was straightforward
- Anthropic Files API + streaming solved both the upload size and idle timeout problems cleanly

**What was hard / wasted time:**
- **Wrong model ID (~45 min):** Used `claude-sonnet-4-20250514` which doesn't exist on this API key. Silent 404, no error logging. Should have tested the API call in isolation BEFORE building the full pipeline.
- **Cloudflare Workers timeouts (~45 min):** `waitUntil` silently dies on free tier. Paid ($5/mo) didn't fix it either. CF Workers are fundamentally wrong for 60-90s AI API calls.
- **Hono node-server timeout (~30 min):** Hono's `@hono/node-server` has a hardcoded ~3 minute connection timeout that can't be overridden. Switching to Express fixed it instantly.
- **Node.js undici socket bug (~30 min):** Node v22's built-in fetch drops connections on large request bodies (known bug, anthropics/anthropic-sdk-typescript#712). Streaming the response fixed it.
- **better-sqlite3 native rebuild (~20 min):** Switching Node versions requires rebuilding native modules. Minor but annoying.

**Key takeaways for future productionize runs:**
1. **Test the AI API call standalone first** — before building any infrastructure around it, verify the model exists, the key works, and a real payload succeeds. A 30-second script would have saved 45 minutes.
2. **Don't use edge/serverless for long-running AI calls** — CF Workers, Vercel Edge, Deno Deploy all have timeout limits that conflict with 60-90s API calls. Use a real server (Railway, Fly.io, VPS).
3. **Express over trendy frameworks for backend APIs** — Hono is great for edge, but Express has 15 years of battle-testing. No surprise timeouts, no compatibility issues.
4. **Streaming prevents idle timeouts** — any AI API call that takes >30s should use streaming to keep the connection alive.
5. **Node 23+ required** for large Anthropic API payloads due to the undici socket bug.

## Next Steps
Run `/productionize:launch` to set up distribution and invite users.
